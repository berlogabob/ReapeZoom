# Source audio and peak caches — the rehearsal WAV is 2 GB, the .reapeaks 74 MB
Media/
*.reapeaks

# REAPER working files -- this repo is the package, not the user's project
*.RPP
*.RPP-bak
*-props.RPP
.DS_Store
